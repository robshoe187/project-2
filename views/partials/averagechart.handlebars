<div class="col-sm-6">
    <canvas id="avgChart" width="400" height="400"></canvas>
</div>


<script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/3.5.1/chart.min.js"></script>
<script src="/javascript/myChart.js"></script>

<script> 

const avgData = {
  labels: ['Player Average', 'League Average'],
  datasets: [{
    label: 'Batting Average',
    backgroundColor: 'rgb(255, 99, 132)',
    borderColor: 'rgb(255, 99, 132)',
    data: [{{ batter.average }}, 218],
  }]
};
const avgConfig = {
  type: 'bar',
  data: avgData,
  options: {
    responsive: false
  }
};
  var avgChart = new Chart(
    document.getElementById('avgChart'),
    avgConfig
  );
  </script>